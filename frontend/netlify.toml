[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Optional: Proxy API requests to your deployed backend manually if needed,
# or user relies on VITE_API_URL env var.
# [[redirects]]
#   from = "/api/*"
#   to = "https://your-render-backend-url.com/api/:splat"
#   status = 200
#   force = true
